import { GridSize } from '../definitions';
export declare class PiecesFactory {
    private _gridSize;
    private _available;
    constructor(_gridSize: GridSize);
    getRandomPiecePiece(x: number, y: number): any;
}
